<?xml version="1.0" encoding="utf-8"?>
<ScrollView
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	tools:context="com.mystatscloud.onpoint.ScheduleTestFormActivity"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:id="@+id/scrollView_Form"
	android:scrollbars="vertical">

	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:paddingBottom="@dimen/activity_vertical_margin"
		android:paddingLeft="@dimen/activity_horizontal_margin"
		android:paddingRight="@dimen/activity_horizontal_margin"
		android:paddingTop="@dimen/activity_vertical_margin"
		android:orientation="vertical"
		android:gravity="bottom">

		<TextView
			android:id="@+id/TextViewTitle"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:text="Complete form to request a test date:"
			android:textSize="@dimen/normalSize"/>

		<EditText
			android:id="@+id/EditTextName"
			android:layout_height="wrap_content"
			android:hint="First &amp; Last Name"
			android:inputType="textPersonName|textCapWords"
			android:layout_width="fill_parent"/>

		<EditText
			android:id="@+id/EditTextPhone"
			android:layout_height="wrap_content"
			android:hint="Phone Number"
			android:inputType="phone"
			android:layout_width="fill_parent"/>

		<EditText
			android:id="@+id/EditTextEmail"
			android:layout_height="wrap_content"
			android:hint="E-mail"
			android:inputType="textEmailAddress"
			android:layout_width="fill_parent"/>

		<EditText
			android:id="@+id/EditTextCompany"
			android:layout_height="wrap_content"
			android:hint="Company"
			android:inputType="text"
			android:layout_width="fill_parent"/>

		<EditText
			android:id="@+id/EditTextMessage"
			android:layout_height="wrap_content"
			android:hint="Comments/Additional Information"
			android:inputType="textMultiLine"
			android:lines="10"
			android:layout_width="fill_parent"
			android:gravity="top" />

		<Button
			android:layout_width="fill_parent"
			android:layout_height="wrap_content"
			android:text="Submit Request"
			android:id="@+id/buttonSubmit"
			android:layout_gravity="center_horizontal"
			android:onClick="submitRequest" />

	</LinearLayout>
</ScrollView>